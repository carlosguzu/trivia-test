// Game configuration constants.

export const TIME_PER_QUESTION = 12; // seconds per question
export const TIMER_UPDATE_INTERVAL = 0.1; // update every 100ms for smooth animation
export const ANSWER_DELAY = 1200; // ms to wait before advancing after an answer
